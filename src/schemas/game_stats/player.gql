type Player {
    name: String!
    position: Position!
    champion: String!
    gold: Int!
    level: Int!
    kills: Int!
    deaths: Int!
    assists: Int!
    farm: Int!
    currentHealth: Int!
    maxHealth: Int!
}

enum Position {
    TOP
    JUNGLE
    MID
    BOTTOM
    SUPPORT
}
