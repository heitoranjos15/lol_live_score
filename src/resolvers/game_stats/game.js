const game = ({ data }) => {
    const { frames = data
    const [ blueTeam, redTeam ] = 
}

    blueTeam: TeamStats!
    redTeam: TeamStats! 
    dragonsTotal: Int!
    baronsTotal: Int!
    towersTotal: Int!
    goldTotal: Int!
    time: String!
    status: String!